on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubunto-latest
      
    steps:
      - name: 'Before'
        run: ls

      - uses: actions/checkout@v3
      
      - name: 'After'
        run: ls

     
